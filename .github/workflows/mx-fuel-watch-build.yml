name: Build MX Fuel Watch PH

# Local builds only. GitHub is kept as the update/distribution channel.
on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  info:
    runs-on: ubuntu-latest
    steps:
      - run: echo "MX Fuel Watch PH is built locally. GitHub Actions auto-build is disabled."
